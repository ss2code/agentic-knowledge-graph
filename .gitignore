# Python
__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Logs
*.log
# -----
CLAUDE.md
.agents/
.claude/
.obsidian/
AGENTS.md
docs/system-overview.md
pwiki.toml

# Neo4j / Data — per-context: ignore only large binary/volume artifacts
data/*/output/
data/*/neo4j_home/
data/*/config/
# debug/ and *.json plan files are kept — small, useful for history/demo

# Safety catch: root-level context artifacts (if data/ is used as a context directly)
data/output/
data/debug/
data/config/
data/*
# Generated DBs (global catch-all)
**/neo4j_home/

# Preserved sample runs (committed for traceability)
# data/sample_runs/ is committed

# Test Artifacts
# tests/canned_data/user_data/ is now committed directly

# Editor
.vscode/

# Git worktrees
.worktrees/
.envrc
.secrets

# pwiki — ephemeral scratch and Obsidian local config
pwiki/raw/
pwiki/.obsidian/
pwiki/*
